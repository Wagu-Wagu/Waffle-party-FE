export default function MiniChip({ ott }: { ott: string }) {
  return (
    <span className="inline-block px-[0.6rem] py-[0.4rem] bg-gray12 text-caption02 Regular rounded-[0.6rem] text-white">
      {ott}
    </span>
  );
}
